import {auth} from "../core/auth.js";
export function render({store}){const u=store.state.user;return `<div class="app-shell"><header class="topbar"><div class="brand"><span>Profil</span></div></header><main class="page"><section class="card"><div class="avatar" style="width:64px;height:64px;font-size:28px">👤</div><h1 class="title">${u?.username||"Operator"}</h1><p class="subtitle">${u?.role||"user"} • Level ${u?.level||1}</p><div style="height:14px"></div><button id="logout" class="btn btn-secondary btn-block">Keluar</button></section></main><nav class="bottom-nav">${nav("/profile")}</nav></div>`}
export function mount(){document.querySelector("#logout")?.addEventListener("click",async()=>{await auth.logout();location.hash="/login"})}
function nav(a){return [["/home","⌂","Home"],["/simulator","🚆","Simulasi"],["/profile","◉","Profil"],["/settings","⚙","Setting"]].map(x=>`<button class="nav-item ${a===x[0]?"active":""}" data-route="${x[0]}"><span class="nav-icon">${x[1]}</span>${x[2]}</button>`).join("")}