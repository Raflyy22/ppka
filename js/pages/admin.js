import {api} from "../core/api.js";
export function render(){const rs=["users","trains","stations","routes","schedules","scenarios","achievements"];return `<div class="app-shell"><header class="topbar"><div class="brand"><img class="brand-icon" src="/assets/icons/logo.svg" alt=""><span>Admin Panel</span></div><button class="icon-btn" data-action="logout">↪</button></header><main class="page"><div class="eyebrow">Management</div><h1 class="title">Control Center</h1><p class="subtitle">Kelola data inti simulator.</p><div style="height:14px"></div><div class="grid grid-2">${rs.map(r=>`<button class="card resource-card" data-resource="${r}" style="text-align:left"><div class="stat-value">${ico(r)}</div><strong>${lbl(r)}</strong><div class="muted">Kelola data</div></button>`).join("")}</div><div style="height:14px"></div><section class="card"><div class="row"><strong id="resource-title">Pilih modul</strong><button class="btn btn-secondary" id="refresh-resource">Refresh</button></div><div id="resource-list" class="empty">Belum memilih modul.</div></section></main><nav id="bottom-nav" class="bottom-nav"></nav></div>`}
export function mount(){let a="";const t=document.querySelector("#resource-title"),l=document.querySelector("#resource-list");async function load(r){a=r;t.textContent=lbl(r);l.innerHTML="Memuat…";try{const x=await api(`/api/${r}`),rows=x.data||x.users||[];l.innerHTML=rows.length?rows.slice(0,30).map(v=>`<div class="list-item"><div class="avatar">${ico(r)}</div><div><strong>${esc(v.name||v.username||v.code||v.email||v.id)}</strong><div class="muted">${esc(v.id)}</div></div></div>`).join(""):`<div class="empty">Belum ada data.</div>`}catch(e){l.innerHTML=`<div class="empty">${esc(e.message)}</div>`}}document.querySelectorAll("[data-resource]").forEach(b=>b.onclick=()=>load(b.dataset.resource));document.querySelector("#refresh-resource").onclick=()=>a&&load(a)}
function lbl(x){return({users:"Users",trains:"Kereta",stations:"Stasiun",routes:"Rute",schedules:"Jadwal",scenarios:"Skenario",achievements:"Achievement"})[x]||x}function ico(x){return({users:"👥",trains:"🚆",stations:"🚉",routes:"🛤️",schedules:"🗓️",scenarios:"🎮",achievements:"🏆"})[x]||"•"}function esc(s){return String(s??"").replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#039;"}[c]))}